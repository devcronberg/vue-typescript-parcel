import Vue from "vue";
import App from "./App.vue";
import Test from "./test.vue";

// tslint:disable-next-line:no-unused-expression
new Vue({
  el: "#app",
  render: h => {
    return h(App);
  }
});
